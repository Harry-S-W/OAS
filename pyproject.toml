[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "oas"
version = "1.0.0"
description = "Oromotor Asymmetry System (OAS)"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
oas = "oas.cli:main"